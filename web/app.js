const $=s=>document.querySelector(s),graph=$('#graph');
function hash(seed){let h=2166136261;for(const c of seed)h=Math.imul(h^c.charCodeAt(0),16777619);return (h>>>0).toString(16).toUpperCase().padStart(8,'0')}
function render(){const events=+$(`#events`).value,rate=+$(`#rate`).value,scenario=$(`#scenario`).value;document.querySelectorAll('output')[0].value=events.toLocaleString();document.querySelectorAll('output')[1].value=rate+'%';$('#m-events').textContent=events.toLocaleString();$('#m-fraud').textContent=Math.round(events*rate/100).toLocaleString();$('#m-entities').textContent=Math.round(Math.sqrt(events)*10).toLocaleString();$('#m-hash').textContent=hash(scenario+events+rate).slice(0,4)+'…'+hash(events+scenario).slice(-4);graph.innerHTML='';for(let i=0;i<Math.min(90,events/15);i++){const n=document.createElement('i');n.className='node '+(i<rate/2?'fraud':'');n.style.left=(5+(i*37)%90)+'%';n.style.top=(8+(i*61)%82)+'%';n.style.transform=`scale(${.6+(i%5)/5})`;graph.appendChild(n)}}
$(`#events`).oninput=render;$(`#rate`).oninput=render;$(`#scenario`).onchange=render;$(`#forge`).onclick=()=>{render();$('#feed').insertAdjacentHTML('afterbegin',`<li><time>NOW</time><span>SEALED</span> ${$(`#scenario`).value} campaign forged and integrity manifest issued</li>`);document.querySelector('.orb').animate([{transform:'scale(.96)'},{transform:'scale(1.04)'},{transform:'scale(1)'}],{duration:700})};render();
